using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//The types of observable objects in the game, this could be expanded later on to add objects such as key cards that AI would notice if they no longer exist.
public enum ObservableType
{
    Door,
    Window,
    Lightswitch
}
